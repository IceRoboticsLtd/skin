<!DOCTYPE html>
<html lang="en">
	<head>
		<% include ../partials/head %>
	</head>
	<body class="container">
		<header>
			<% include ../partials/header %>
		</header>
		<main>
			<div id="formContainer">
				<% if (message) { %>
					<div class="alert alert-danger"><%= message %></div>
				<% } %>
				<form id="login" method="post" action="/login" role="form">
					<a href="#" id="flipToRecover" class="flipLink"><%= __("authen.forgot") %></a>
					<div class="form-group">
						<label for="username"><%= __("authen.username") %></label>
						<input id="username" type="text" name="username" placeholder="<%= __("authen.username") %>" />
					</div>
					<div class="form-group">
						<label for="password"><%= __("authen.password") %></label>
						<input id="password" type="text" name="password" placeholder="<%= __("authen.password") %>" />
					</div>
					<div class="checkbox">
						<label>
							<input type="checkbox"><%= __("authen.remember_me") %>
						</label>
					</div>
						<button type="submit" name="submit"><%= __("authen.log_in") %></button>
				</form>
				<form id="recover" method="post" action="./login">
					<a href="#" id="flipToLogin" class="flipLink"><%= __("authen.forgot") %></a>
					<div class="form-group">
						<label for="recoverEmail"><%= __("authen.recover_email") %></label>
						<input id="recoverEmail" type="text" name="recover_email" placeholder="<%= __("authen.recover_email") %>" />
					</div>
						<button type="submit" name="submit"><%= __("authen.recover") %></button>
				</form>
			</div>
		</main>
		<footer>
			<% include ../partials/footer %>
		</footer>
	</body>
</html>		