# First clone the following repository into this directory, named 'postal', like so:

git clone https://github.com/postaljs/postal.js.git postal

# Next time, when in need of an update, pull (= combines fetch and merge) the same repository from within the 'postal' directory simply like so:

git pull
