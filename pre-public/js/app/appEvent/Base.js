/*
 * AppEventBase
 */
define(function () {
    console.log('SKIN: appEventBase called');
    function appEventBase(id) {
        this.id = id;
    };
    appEventBase.prototype = {
		// to do
    };
    return appEventBase;
});
