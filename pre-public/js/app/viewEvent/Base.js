/*
 * ViewEventBase
 */
define(function () {
    console.log('SKIN: viewEventBase called'); 	
    function viewEventBase(id) {
        this.id = id;
    }
    viewEventBase.prototype = {
		// to do
    };
    return viewEventBase;
});
