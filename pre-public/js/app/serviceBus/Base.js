/*
 * ServiceBusBase
 */
define(function () {
    console.log('SKIN: serviceBusBase called');	
    function serviceBusBase(id) {
        this.id = id;
    };
    serviceBusBase.prototype = {
		// to do
    };
    return serviceBusBase;
});
